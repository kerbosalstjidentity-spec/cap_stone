# 실험 산출물 (`outputs/`)

데이터셋·실험 종류별로 하위 폴더에 둡니다.

| 폴더 | 내용 |
|------|------|
| `creditcard/` | Kaggle `creditcard.csv` — 비지도 요약, IF+RF/LGBM 하이브리드, CV 비교 |
| `ieee_cis/` | IEEE-CIS 파이프라인 — 메트릭 CSV, `EXPERIMENT_LOG.md`, `operational/`·`walk_forward/` 등 |

스크립트는 위 경로에만 쓰도록 맞춰 두었습니다. (이전 루트의 `ieee_cis_*.csv` 등은 이 구조로 이전됨.)
