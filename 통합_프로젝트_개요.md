# 통합 프로젝트 개요 — PayWise + 3축 접근통제

> **두 개의 독립 서비스가 하나의 플랫폼을 이룬다.**
> 사기를 *탐지*하는 본 시스템(PayWise)과, 그 판정 데이터의 *접근을 거버넌스*하는 보안 레이어(3축 접근통제).
> 마이크로서비스 형태 — 각자 독립 배포하되 명확한 연결지점으로 묶임(통합 수준 2).

---

## 1. 구성 (두 프로젝트)

| | **PayWise** (메인 플랫폼) | **3축 접근통제** (보안 레이어) |
|---|---|---|
| 폴더 | `캡스톤_프로젝트/` | `캡스톤_프로젝트_후속/` |
| GitHub | `kerbosalstjidentity-spec/cap_stone` | `kerbosalstjidentity-spec/fraud-access-control` |
| 역할 | 거래수집·ML 사기탐지·자산/소비분석·교육 | 사기 판정 데이터 칸단위 접근통제 + 단계 인증 |
| 스택 | Next.js · FastAPI · PostgreSQL · Redis | FastAPI · SQLite · Jinja2 |
| 데이터 | PaySim (거래) | Sparkov (실명·카드·주소 PII 포함) |
| 배포 | Vercel(프론트) + Render(백엔드/추론/DB) | Render(단일 웹서비스) |

---

## 2. 아키텍처 / 연결지점

```
[Vercel] PayWise 프론트(Next.js)
     │  └─ 상단 메뉴 "🔐 사기 접근통제" 링크 ───────────┐
     ▼                                                  ▼
[Render] PayWise 백엔드(FastAPI)              [Render] 3축 접근통제 데모(FastAPI)
     ├─ Postgres / Redis                          └─ 페르소나×구역×동작 정책 엔진
     └─ fraud-service (ML 사기탐지)                    + 응답 필터(복호화/마스킹) + step-up
          │                                              ▲
          └────── 논리적 연결: "사기 판정 데이터" ────────┘
                  (PayWise가 탐지 → 후속이 접근 거버넌스)
```

- **연결지점(구현):** PayWise 프론트 NavBar 의 외부 링크 → 후속 데모(새 탭)
- **연결지점(서사):** PayWise가 사기를 *탐지*하고, 후속 3축 레이어가 그 판정 데이터를 *누가·무엇을·어떻게* 보는지 칸 단위로 통제

---

## 3. 배포 URL

| 대상 | URL |
|---|---|
| PayWise (메인) | https://cap-stone-neon.vercel.app |
| PayWise 백엔드 API | https://paywise-backend-asa1.onrender.com/docs |
| 3축 접근통제 데모 | https://fraud-access-control.onrender.com |

> ⚠️ 무료티어 슬립(15분 미사용 → 첫 접속 ~50초). **발표 직전 세 URL 미리 한 번씩 깨워둘 것.**

---

## 4. 로컬 실행 / 배포 문서

| 프로젝트 | 실행/배포 가이드 |
|---|---|
| PayWise | `docs/DEPLOY.md` (Vercel+Render), `README.md`, `docker-compose up` |
| 3축 접근통제 | `캡스톤_프로젝트_후속/docs/DEMO_RUNBOOK.md` (로컬/Docker), `docs/DEPLOY 흐름은 단일 Render 서비스` |
| 접근통제 매트릭스 | `캡스톤_프로젝트_후속/docs/PERMIT_조합.md` (데모용 PERMIT 조합) |
| 보안 설계 정본 | `캡스톤_프로젝트_후속/docs/NEW_DESIGN_사기판정_접근통제_3축.md` |

---

## 5. 통합 수준 (왜 한 폴더로 안 합쳤나)

**수준 2 = "독립 배포 + UI 링크 + 서사"** 를 선택.
- 두 서비스는 스택·데이터·목적이 달라 물리적 단일 코드베이스로 합치면 복잡도·리스크만 큼.
- 마이크로서비스로 분리하고 연결지점만 명확히 두는 게 운영·발표 양쪽에 유리.
- 깊은 데이터 결합(후속이 PayWise API를 직접 읽음)은 스키마 차이(PaySim↔Sparkov)로 향후 과제.
